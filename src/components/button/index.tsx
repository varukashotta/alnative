import React from 'react';
import {TouchableOpacity, Text} from 'react-native';

const Button = () => {
  return (
    <TouchableOpacity>
      <Text>Test</Text>
    </TouchableOpacity>
  );
};

export default Button;
